#include<stdio.h>

/*Write a C program to sort first half in ascending and second half in decending order

 solution:-Given an array of integer, sort the first half of the array in ascendind order and second 
    half in desecnding
    
    Input 
    arr[]={5,2,4,7,9,3,1,6,8} 
    
    Output:-arr[]={1,2,3,4,9,8,7,6,5} */

  void sort(int arr[],int n);
  void printOder(int arr[],int n);